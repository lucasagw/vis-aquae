import 'package:vis_aquae/shared/models/device.dart';

class DeviceResidence {
  final String idResidence;
  final Device device;
  final String tempoLigado;

  DeviceResidence(this.idResidence, this.device, this.tempoLigado);
}
